{
  "name": "sajumon-root",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd api && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": "24.x",
    "npm": "11.x"
  }
}